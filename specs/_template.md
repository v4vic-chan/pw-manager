# [專案名稱] — 中粒度系統規格 v[版本號]

<!--
本文件為跨專案通用「規格骨架模板」。
用途：確保每個專案的規格文件章節標題一致，讓 .claude/commands/ 下的
slash command 能用「語意標題比對」而非「章節編號」定位內容，達成跨專案複用。

使用規則：
1. 以下 0～7 節為固定骨架，標題文字不可更動（可加副標，但主標題需保留關鍵字）。
2. 每節內的子標題（如 4.1、4.2...）由該專案自行決定，依實際功能模組命名。
3. 若某節對該專案不適用（例如非安全敏感型專案不需要加密規範），
   仍保留標題，內容註明「不適用」，不可整節刪除，以維持章節結構穩定。
4. 本模板骨架與專案採用的技術棧無關，換不同技術棧的新專案時，
   骨架標題不需要修改，只需在對應章節填入該專案的實際內容。
-->

## 0. 文件定位
說明本文件的規格定位與範圍（通常為固定樣板文字，各專案大致相同）：
本文件為「中粒度契約規格」，定義模組邊界、資料契約、約束條件、驗收標準。
不包含演算法內部實作細節，由實作端依本文件訂下的規則自行完成。

## 1. 專案範圍與非目標

### 1.1 範圍
（列出本專案要做什麼，含平台形式、使用情境、核心功能清單）

### 1.2 非目標（Out of Scope）
（明確排除不做的項目，避免規格蔓延）

## 2. 系統架構總覽

### 2.1 技術棧選型
（列出本專案採用的技術棧，建議以表格呈現：層級／選型／選型理由，
無論專案性質為何皆需填寫此節，內容依專案實際決定）

### 2.2 分層架構
（描述系統分層，如 UI 層／應用邏輯層／資料存取層／其他專案特有層）

### 2.3 資料流向硬性約束
（跨層呼叫的禁止事項、資料必須經過哪些層才能落地等強制規則）

## 3. 資料契約（Data Contracts）
（列出核心資料結構，每個結構用表格描述欄位、型態、必填、約束）

## 4. 功能模組規格
（依專案實際功能拆分子章節，如 4.1、4.2...，每個子章節描述該模組的
輸入輸出、流程、邊界條件；子標題命名完全依專案內容決定）

## 5. 非功能需求與品質約束
（依專案性質決定包含哪些子項，若不適用需保留標題並註明「不適用」）

### 5.1 安全與加密規範（如適用）
### 5.2 效能與相容性要求（如適用）
### 5.3 其他品質約束（如適用）

## 6. 驗收標準（Acceptance Criteria）
（條列可被測試明確驗證的驗收項目，供跨層邊界測試／單元測試依循，
每項須具體可判定「過」或「不過」，不可用模糊描述；
若涉及多層架構間的資料一致性驗證，應在此標明供邊界測試對照）

## 7. 待實作端決定事項
（刻意留白，列出型態檔組織方式、狀態管理等交由 CLI 執行端
依技術棧細節自行決定的次要事項；技術棧本身的選型已在 §2.1 定案，
此節僅保留真正次要、不影響架構的實作細節）
